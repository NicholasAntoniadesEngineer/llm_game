# Python: use 3.11 or 3.12 for this lockfile (wheels for pydantic-core). 3.14 often fails to build.
#   python3.11 -m venv .venv && .venv/bin/pip install -r requirements.txt && .venv/bin/python main.py

# Core web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0

# File watching for development
watchfiles==0.21.0

# Data validation and serialization
pydantic==2.5.0
pydantic-settings==2.1.0

# JSON schema validation for society files
jsonschema==4.19.2

# Async HTTP client for external APIs
httpx==0.25.2

# Environment variable management
python-dotenv==1.0.0

# Process / system metrics
psutil==5.9.6

# Tests
pytest==7.4.3
pytest-asyncio==0.21.1
